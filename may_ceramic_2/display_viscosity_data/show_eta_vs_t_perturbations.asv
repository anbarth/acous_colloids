myColor = @(voltage) cmap(round(1+255*voltage/80),:);
volt_list = [0 5 10 20 40 60 80];

dataTable = may_ceramic_09_17;

V = 60;
phi_list = unique(dataTable(:,1));
phi = phi_list(8);

figure; makeAxesLogLog;
for jj=2:length(volt_list)
    V = volt_list(jj);
    [eta,t,tStart] = findThisPerturbationDisplayInfo(0.48,1,5,{phi48_05_31});
    plot(t-tStart,eta,'-','Color',myColor(V));
end